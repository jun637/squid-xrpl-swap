# XRPL testnet RLUSD issuer address
ISSUER_ADDRESS=

# Wallet seed that pays + receives (same-chain). Must have an RLUSD trustline set.
RECIPIENT_SEED=

# 테스트 지갑/issuer의 실제 값은 핸드오프 문서를 참조하세요.
# (testnet 전용, 가치 0, 메인넷 재사용 금지)
